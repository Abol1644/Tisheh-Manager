export interface Inventory {
  outStock: boolean;
  unlimitedInventory: boolean;
  warehouse: number;
  escrowBalance: number;
  fullInvestory: number;
  inventorySupplyWarehouse: boolean;
  inventorySupplyLocationStr: string;
  inventoryStr: string;
}
